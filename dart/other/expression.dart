///Dart 表达式不能直接包含语句
main() {
  var num = 6;
  var val = num > 10 ? "大于10" : "小于等于10";
  print(val); //小于等于10

//  var err1 = i=0 ? "v1": "v2";//报错，表达式不能为语句
//  if (i=0) {}//报错，表达式不能为语句
  var err2 = true ? "v1": "v2";//表达式允许直接使用 bool 值，严格来说 ture 也算语句，所以重要概念中有些地方说法不太严谨(个人愚见)
}